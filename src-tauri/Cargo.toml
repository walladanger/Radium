[package]
name = "Atomic-Chat"
version = "0.6.599"
description = "Use offline LLMs with your own data. Run open source models like Llama2 or Falcon on your internal computers/servers."
authors = ["Jan <service@jan.ai>"]
license = "MIT"
repository = "https://github.com/janhq/jan"
edition = "2021"
# Raised from 1.77.2 on 2026-09-10 by Task 18. Credential storage requires it:
# keyring-core needs 1.85 and the platform backends need 1.88. The old value
# had become false - a build on an older toolchain would fail anyway, just
# with a confusing resolver error instead of "requires rustc 1.88".
rust-version = "1.88"
resolver = "2"
default-run = "Atomic-Chat"
autobins = false

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "Atomic-Chat"
path = "src/main.rs"

[[bin]]
name = "jan-cli"
path = "src/bin/jan-cli.rs"
required-features = ["cli"]

[[example]]
name = "gaia-eval"
path = "examples/gaia-eval.rs"
required-features = ["gaia-eval"]

[features]
default = [
    "tauri/wry",
    "tauri/common-controls-v6",
    "tauri/x11",
    "tauri/protocol-asset",
    "tauri/macos-private-api",
    "tauri/tray-icon",
    "tauri/test",
    "tauri/custom-protocol",
    "desktop",
]
hardware = ["dep:tauri-plugin-hardware"]
deep-link = ["dep:tauri-plugin-deep-link"]
mlx = ["dep:tauri-plugin-mlx"]
foundation-models = ["dep:tauri-plugin-foundation-models"]
desktop = [
    "deep-link",
    "hardware",
    "mlx",
    "foundation-models"
]
mobile = [
    "tauri/protocol-asset",
    "tauri/test",
    "tauri/wry",
    "dep:sqlx",
]
test-tauri = [
    "tauri/wry",
    "tauri/x11",
    "tauri/protocol-asset",
    "tauri/macos-private-api",
    "tauri/tray-icon",
    "tauri/test",
    "desktop",
]
cli = ["dep:env_logger", "dep:dialoguer", "dep:indicatif", "dep:console"]
gaia-eval = ["dep:env_logger", "dep:indicatif", "dep:parquet"]

[build-dependencies]
tauri-build = { version = "2.0.2", features = [] }

[dependencies]
dirs = "6.0.0"
async-trait = "0.1"
env = "1.0.1"
fix-path-env = { git = "https://github.com/tauri-apps/fix-path-env-rs" }
flate2 = "1.0"
futures-util = "0.3.31"
hex = "0.4"
hmac = "0.12"
hyper = { version = "0.14", features = ["server"] }
atomic-code-index = { path = "./code-index" }
jan-utils = { path = "./utils" }
libloading = "0.8.7"
log = "0.4"
png = "0.18"
portable-pty = "0.9"
rand = "0.8"
rmcp = { version = "0.8.5", features = [
    "client",
    "transport-sse-client",
    "transport-sse-client-reqwest",
    "transport-streamable-http-client",
    "transport-streamable-http-client-reqwest",
    "transport-child-process",
    "tower",
    "reqwest",
    "auth",
] }
# Same version rmcp's `auth` feature resolves to; direct dep only for the
# `TokenResponse` trait methods on the token type rmcp hands back.
oauth2 = "5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
json5 = "0.4"
serde_yaml = "0.9.34"
sha2 = "0.10"
tar = "0.4"
zip = "0.6"
tauri-plugin-deep-link = { version = "2", optional = true }
tauri-plugin-hardware = { path = "./plugins/tauri-plugin-hardware", optional = true }
tauri-plugin-llamacpp = { path = "./plugins/tauri-plugin-llamacpp" }
tauri-plugin-llamacpp-upstream = { path = "./plugins/tauri-plugin-llamacpp-upstream" }
tauri-plugin-mlx = { path = "./plugins/tauri-plugin-mlx", optional = true }
tauri-plugin-foundation-models = { path = "./plugins/tauri-plugin-foundation-models", optional = true }
tauri-plugin-vector-db = { path = "./plugins/tauri-plugin-vector-db" }
tauri-plugin-rag = { path = "./plugins/tauri-plugin-rag" }
tauri-plugin-atomic-audio = { path = "./plugins/tauri-plugin-atomic-audio" }
tauri-plugin-http = { version = "2", features = ["unsafe-headers"] }
tauri-plugin-log = "2.0.0-rc"
tauri-plugin-notification = "2"
tauri-plugin-opener = "2.2.7"
tauri-plugin-os = "2.2.1"
tauri-plugin-shell = "2.2.0"
tauri-plugin-store = "2"
sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite"], optional = true }
thiserror = "2.0.12"
tokio = { version = "1", features = ["full"] }
tokio-util = "0.7.14"
url = "2.5"
uuid = { version = "1.7", features = ["v4"] }
rfd = { version = "0.15.4", default-features = false, features = [
  "tokio",
  "xdg-portal",
  "common-controls-v6",
] }
chrono = { version = "0.4", features = ["serde"] }
ed25519-dalek = { version = "2.1", features = ["rand_core"] }
futures = "0.3"
sysinfo = "0.34.2"
hostname = "0.4"
base64 = "0.22"
glob = "0.3"
clap = { version = "4", features = ["derive", "env"] }
dialoguer = { version = "0.11", optional = true }
env_logger = { version = "0.11", optional = true }
indicatif = { version = "0.17", optional = true }
console = { version = "0.15", optional = true }
ignore = "=0.4.30"
regex = "1.13.1"
diffy = "0.4.2"
trash = "=5.2.3"
parquet = { version = "54.3.1", default-features = false, features = ["brotli", "flate2", "json", "lz4", "snap", "zstd"], optional = true }

[dependencies.tauri]
version = "2.8.5"
default-features = false
features = [
  "protocol-asset",
  "macos-private-api",
  "test",
  "devtools",
  "image-png",
]

[dev-dependencies]
tempfile = "3.20.0"

[target.'cfg(target_os = "macos")'.dependencies]
apple-native-keyring-store = "1"

[target.'cfg(unix)'.dependencies]
nix = { version = "=0.30.1", features = ["signal", "process", "resource"] }
libc = "0.2"

[target.'cfg(target_os = "linux")'.dependencies]
# zbus, NOT dbus: the dbus backend pulls libdbus-sys, which links the system
# C library and would make libdbus-1-dev a NEW build requirement for Linux.
# The zbus backend is pure Rust and this crate already depends on zbus 5
# below, so this adds no system dependency. Q5 approved either one.
zbus-secret-service-keyring-store = "1"
# zbus-secret-service-keyring-store 1.0.1 disables secret-service's crypto
# defaults, so Linux must select one crypto implementation explicitly. Feature
# unification makes this apply to the store's transitive secret-service crate.
secret-service = { version = "5.2", default-features = false, features = ["crypto-rust"] }
zbus = { version = "5", default-features = false, features = ["tokio"] }
notify-rust = "4"

[target.'cfg(windows)'.dependencies]
windows-native-keyring-store = "1"
libc = "0.2.172"
windows-sys = { version = "0.60.2", features = ["Win32_Storage_FileSystem", "Win32_Globalization"] }
winreg = "0.52"

[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
# ATO / Radium Media: provider API keys live in the OS credential store, never
# in localStorage. See docs/decisions/2026-09-10-store-media-provider-
# credentials-in-the-os-credential-store.md. These four crates are the ones the
# user approved by name under AGENTS.md rule 6 (tracker decision Q5); the
# approval is scoped to credential storage only.
# keyring-core, NOT the `keyring` crate: at 4.2 that one is the all-in-one
# CLI (`pub use cli::*`) and pulling it in as a library would drag the CLI's
# dependency tree into the app. keyring-core 1.0 is the library the three
# approved platform stores already depend on, and is what Q5's "keyring 4.2
# (core)" meant. Same family, same approval, fewer dependencies.
keyring-core = "1"
reqwest = { version = "0.11", features = ["json", "blocking", "stream", "native-tls-vendored"] }
once_cell = "1.18"
tauri-plugin-single-instance = { version = "2", features = ["deep-link"] }
tauri-plugin-autostart = "2.5.1"
# GH #45: persist & restore the main window's size/position across restarts.
tauri-plugin-window-state = "2"
# ATO-113: Sentry crash/error telemetry (desktop only; never on mobile). The
# `log` feature bridges `log::error!` -> Sentry via SentryLogger; default
# features bring the panic hook, backtraces, contexts, and the reqwest/native-tls
# transport.
sentry = { version = "0.34", features = ["log"] }

[target.'cfg(any(target_os = "android", target_os = "ios"))'.dependencies]
reqwest = { version = "0.11", features = ["json", "blocking", "stream", "rustls-tls"], default-features = false }
tauri-plugin-dialog = { version = "2.2.1", default-features = false }
tauri-plugin-http = { version = "2", default-features = false }
tauri-plugin-log = { version = "2.0.0-rc", default-features = false }
tauri-plugin-opener = { version = "2.2.7", default-features = false }
tauri-plugin-os = { version = "2.2.1", default-features = false }
tauri-plugin-shell = { version = "2.2.0", default-features = false }
tauri-plugin-store = { version = "2", default-features = false }

# Release profile optimizations for minimal binary size
[profile.release]
opt-level = "z"     # Optimize for size
lto = "fat"         # Aggressive Link Time Optimization
strip = "symbols"   # Strip debug symbols from the SHIPPED binary (kept small)
codegen-units = 1   # Reduce parallel codegen for better optimization
panic = "abort"     # Don't unwind on panic, saves space (panic hook runs first)
overflow-checks = false  # Disable overflow checks for size
# ATO-113: emit debug info into a SEPARATE file (macOS .dSYM / Linux .dwp /
# Windows .pdb) so `sentry-cli debug-files upload` can symbolicate Rust crash
# stacks, while `strip = "symbols"` keeps the bundled executable stripped.
debug = 1           # line tables (enough for symbolication)
split-debuginfo = "packed"
debug-assertions = false  # No debug assertions
incremental = false # Disable incremental compilation for release
rpath = false       # Don't include rpath
